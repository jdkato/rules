---
message: "Don't say '%s'"
extends: existence
ignorecase: false
description: |
  An example of targeting multiple scopes.

  category: scoping

  example:

  # Test at level `h1`

  ## Test at level `h2`

  ### Test at level `h3`
# You can now provide an array of scopes to target.
scope:
  - heading.h1
  - heading.h3
level: error
tokens:
  - Test
